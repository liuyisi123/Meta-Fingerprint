# Split Manifests

Place patient-disjoint split manifests here when sharing review-safe metadata.

The public repository provides schemas under `examples/manifests/`. Actual split
manifests derived from restricted cohorts should be shared only when allowed by
the corresponding data-use agreements.
