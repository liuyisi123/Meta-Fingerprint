# Examples

This folder contains review-safe templates. It intentionally contains no
clinical data.

- `manifests/split_manifest_template.csv`: patient-disjoint split schema.
- `manifests/setting_protocol_template.csv`: Setting A-D protocol metadata.

Use these files to document the exact split and protocol used for each run.
