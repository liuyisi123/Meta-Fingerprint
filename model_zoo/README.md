# Model Zoo

Place trained checkpoints here for private reviewer packages or public releases
when redistribution is allowed.

Recommended names:

- `metafingerprint_vitaldb_task_b_best.pt`
- `metafingerprint_vitaldb_task_a_best.pt`
- `metafingerprint_public_setting_a_best.pt`

Do not commit checkpoints trained with restricted RWW support/query data unless
the data-use agreement explicitly permits release.
